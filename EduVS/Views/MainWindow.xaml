<Window x:Class="EduVS.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EduVS.ViewModels"
        xmlns:views="clr-namespace:EduVS.Views"
        mc:Ignorable="d"
        Title="EduVS" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" MinWidth="120"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="5">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="Margin" Value="0,4,0,0"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                </Style>
            </StackPanel.Resources>

            <Button Content="Create new test" Command="{Binding ShowGenerateTestWindowCommand}"/>
            <Separator Margin="0,10"/>
            <Button Content="Prepare test check" Command="{Binding ShowPrepareTestCheckWindowCommand}"/>
            <Separator Margin="0,10"/>
            <Button Content="Export test results" Command="{Binding ShowGenerateTestResultsWindowCommand}"/>
        </StackPanel>

        <GridSplitter Grid.Column="1"
                      Width="5"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ResizeDirection="Columns"
                      ResizeBehavior="PreviousAndNext"
                      ShowsPreview="True"
                      Background="LightGray"/>
    </Grid>
</Window>
